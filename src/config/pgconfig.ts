// import dotenv from "dotenv";
// import { Client } from "pg";

// dotenv.config();

// const isProduction = process.env.NODE_ENV === "production";
// const pgp = pgPromise({});
// const cn = isProduction
//     ? process.env.DATABASE_URL
//     :
// `postgresql://${process.env.DB_USER}:
// ${process.env.DB_PASSWORD}@${process.
// env.DB_HOST}:${process.env.DB_PORT}/${process.env.DB_DATABASE}`;

// if (!cn) {
//     throw new Error("Db connection istring is undefined");
// }
